import { OperateDirective } from './operate.directive';

describe('OperateDirective', () => {
  it('should create an instance', () => {
    const directive = new OperateDirective();
    expect(directive).toBeTruthy();
  });
});
